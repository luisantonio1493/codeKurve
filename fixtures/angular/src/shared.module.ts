// Referenced only from `imports` arrays (app.module.ts's `@NgModule` and
// invoice.component.ts's standalone `imports`) — no decorator of its own
// needed for those `RegisteredAs` edges to resolve to a real symbol.
export class SharedModule {}
